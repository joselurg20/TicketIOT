export enum Priorities {
    NOT_SURE = 0,
    LOWEST = 1,
    LOW = 2,
    MEDIUM = 3,
    HIGH = 4,
    HIGHEST = 5
}

export enum Status {
    OPENED = 0,
    PAUSED = 1,
    PENDING = 2,
    FINISHED = 3
}